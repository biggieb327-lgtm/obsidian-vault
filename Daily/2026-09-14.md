---
title: "2026-09-14"
date: "2026-09-14"
tags:
  - daily
  - session-log
  - governance
---

# 2026-09-14 — Session Log & System State

## Overview
Dense operational session. The user re-ran `hermes portal`, which **broke Hindsight**
(auth-token rotation desynced the container), then gave root access to apply the
durable fix. Substantial hardening, three feature requests filed upstream, two
token-saving skills installed, and the GPT-6 Astra principles adopted into the
core instructions.

> **Status: everything below marked COMPLETE is verified, not just claimed.**

---

## Completed this session

### 1. Hindsight single-auth (the big one)
- `hermes portal` rotation invalidated the container's separate auth copy → Hindsight
  LLM ops failed (`invalid_grant`) while `/health` stayed green (a silent failure).
- **Durable fix applied:** Hindsight container now mounts the **host's live
  `auth.json` read-only** (`-v ~/.hermes/auth.json:/home/hindsight/.hermes/auth.json:ro`),
  per the documented setup. One credential, one rotation, no desync.
- `hindsight_single_auth.sh` = the migration script (run as root). Data volume
  `hindsight-data` preserved; env/restart intact. Verified `/health` + LLM path (200).
- Sync cron (`c08ce685de9a`) **paused** and `hindsight-auth-sync` mechanism removed
  (obsolete). Decision `dec_027`.

### 2. Docker access for `hermes`
- User added `hermes` to the docker group. The **gateway process predates the change**,
  so use `sg docker -c "..."` until the gateway restarts.

### 3. Vault auto-push bug (silent backup failure) — FIXED
- `obsidian_auto_push.sh` used `git diff` which ignores **untracked** files → new
  daily notes never reached GitHub. Now uses `git status --porcelain`. Negative control
  confirmed (a new untracked file now commits+pushes).

### 4. Token-saving skills + monthly sync
- **`caveman`** (prose compression) + **`ponytail`** (code minimalism) installed from
  GitHub (`JuliusBrussee/caveman`, `DietrichGebert/ponytail`), adapted to Hermes format.
- **Monthly sync cron** (1st 03:00, `skill_sync.py`) pulls latest upstream bodies,
  preserves Hermes frontmatter. Idempotent + drift-repair verified.

### 5. Reddit feature requests — filed upstream
- Read the "Missing features ideas" post; filed as Hermes GitHub issues:
  - **#110427** soft-timeout for background tasks (wake agent, don't hard-kill)
  - **#110428** git-compatible secret-safe export/restore
  - **#110429** approval-required persistent changes

### 6. Reproducible setup
- `repro_backup.sh` commits a **secret-free** instance snapshot to private repo
  `biggieb327-lgtm/hermes-profile` (config, SOUL, AGENTS, profiles, mechanisms,
  plugins, memories, cron data). Daily cron 02:30. Decision `dec_023`.

### 7. Control-file approval guard
- `guard_control_files.py` baselines 20 control files; any drift = unapproved change
  until `--accept`. Caught `hermes portal` writing `config.yaml` (working as intended).
  Decision `dec_024`.

### 8. Notion token — ROTATED
- User provided a new token; old one revoked (was baked into 62 files). New key in
  `~/.hermes/.env` + `profiles/treasury/.env`. Live-tested (Notion API 200), no git
  leak, reminder cron removed. Decision `dec_028`.

### 9. GPT-6 Astra principles — adopted (dec_029)
- **SOUL.md**: new `## Initiative & Follow-Through` (bias toward action, define done,
  persist, ask only when it changes the outcome).
- **AGENTS.md**: testing calibration (§2 Tier 2) + new `## 14. Instruction & Skill
  Hygiene` (progressive disclosure, short contextual descriptions).
- Mirrored to vault + repro; mechanism audit 16/16.

---

## Decisions logged this session
`dec_023` repro snapshot · `dec_024` control-file guard · `dec_025` hindsight auth
sync (obsolete) · `dec_027` single-auth live mount · `dec_028` Notion rotation ·
`dec_029` Astra principles.

---

## Action Items & Next Steps

- [ ] **YNAB Chase Checking reconciliation** (carried from 09-13) — $882.72 unrecorded
      outflows outstanding. The only genuinely open item; everything else closed.

---

## Reminders for the next session
- **After any `hermes portal` run, restart the gateway** so the host process reloads
  the token (a running gateway holds a stale in-memory refresh token → `invalid_grant`
  → quarantine). The container is immune now (live mount); the host is not.
- Docker commands: prefix with `sg docker -c "..."` until the gateway restarts.

---

## Infrastructure Snapshot (verified ~05:25 UTC)
| Check | Result |
|---|---|
| Hindsight `/health` + LLM path | healthy; single-auth live mount active |
| Mechanism audit | 16/16 PASS |
| Cron doctor | no issues (26 jobs) |
| Gateway | restarted 04:19 UTC, zero auth errors since |
| Notion API | HTTP 200 (new token) |
| Repos (scripts/repro/vault) | clean, pushed |
| Secret scan | clean |

## Notable tool learnings (non-fatal)
- `dry-run-extract` payload shape is `{"content": ...}`, not `{"text": ...}`.
- Cron jobs must use `terminal`, not `execute_code` (blocked in cron context).
- `hermes cron` uses `edit`, not `update`.

---

[[Memory-Wiki]] | [[Decisions-Log]] | [[WORKSPACE_MAP]] | [[Content-Calendar]]
